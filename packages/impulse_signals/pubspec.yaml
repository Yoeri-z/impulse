name: impulse_signals
description: "Impulse together with signals."
version: 0.1.2
homepage: https://github.com/Yoeri-z/impulse
issue_tracker: https://github.com/Yoeri-z/impulse/issues
topics:
  - state
  - signals

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  impulse_flutter: ^0.1.1
  signals_flutter: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

flutter:
